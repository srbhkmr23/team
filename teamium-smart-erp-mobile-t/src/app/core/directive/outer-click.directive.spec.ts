import { OuterClickDirective } from './outer-click.directive';

describe('OuterClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OuterClickDirective();
    expect(directive).toBeTruthy();
  });
});
