import { CurrencySearchPipe } from './currency-search.pipe';

describe('CurrencySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
