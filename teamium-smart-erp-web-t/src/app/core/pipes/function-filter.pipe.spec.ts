import { FunctionFilterPipe } from './function-filter.pipe';

describe('FunctionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FunctionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
