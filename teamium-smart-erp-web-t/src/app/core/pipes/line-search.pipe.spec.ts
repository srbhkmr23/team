import { LineSearchPipe } from './line-search.pipe';

describe('LineSearchPipe', () => {
  it('create an instance', () => {
    //const pipe = new LineSearchPipe();
   // expect(pipe).toBeTruthy();
  });
});
