import { ShowProjectFilterPipe } from './show-project-filter.pipe';

describe('ShowProjectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowProjectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
