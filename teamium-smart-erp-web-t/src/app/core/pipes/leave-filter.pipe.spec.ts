import { LeaveFilterPipe } from './leave-filter.pipe';

describe('LeaveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
