import { GlobalSearchPipePipe } from './global-search-pipe.pipe';

describe('GlobalSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
