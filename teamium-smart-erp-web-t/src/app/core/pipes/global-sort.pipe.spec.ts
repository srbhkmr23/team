import { GlobalSortPipe } from './global-sort.pipe';

describe('GlobalSortPipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalSortPipe();
    expect(pipe).toBeTruthy();
  });
});
