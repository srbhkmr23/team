import { EquipmentFilterPipe } from './equipment-filter.pipe';

describe('EquipmentSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
