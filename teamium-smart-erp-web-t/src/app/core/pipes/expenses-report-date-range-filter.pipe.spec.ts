import { ExpensesReportDateRangeFilterPipe } from './expenses-report-date-range-filter.pipe';

describe('ExpensesReportDateRangeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpensesReportDateRangeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
