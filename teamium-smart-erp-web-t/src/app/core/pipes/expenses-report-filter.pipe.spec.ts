import { ExpensesReportFilterPipe } from './expenses-report-filter.pipe';

describe('ExpensesReportFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpensesReportFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
