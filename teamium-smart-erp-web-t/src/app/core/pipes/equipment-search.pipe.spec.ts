import { EquipmentSearchPipe } from './equipment-search.pipe';

describe('EquipmentSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
